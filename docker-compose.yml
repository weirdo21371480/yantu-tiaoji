services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - JWT_SECRET=change-this-to-a-random-secret
      - NODE_ENV=production
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
